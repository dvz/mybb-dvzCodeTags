# DVZ Code Tags

A MyBB 1.8.x plugin for parsing code tags:

`` `backticks` ``
````
```
fenced blocks
```
````
```
[code=language]
MyCode blocks with optional language specified
[/code]
```

**Requirements:**
- PHP >= 7.1
